

{% extends 'base.html.twig' %}

{% block body %}

  {% for msg in app.session.flashBag.get('success') %}
        <div class="alert alert-success">
            {{ msg }}
        </div>
    {% endfor %}


    <a href="{{ path('article-creat') }}"> <button type="button" class="btn btn-primary" style="margin-left:110px; margin-top: 50px; margin-bottom: 50px">stworz artykuł</button> </a>

    <div class="container color-fonts">
<table class="table table-striped">
    <tr>
        <th> id      </th>
        <th> title   </th>
        <th>  text   </th>
        <th>  Edit   </th>
        <th>  Cancel </th>
    </tr>

    {%   for article in articles  %}


<tr>
    <td>
    {{   article.id  }}
    </td>
    <td>
   {{  article.title }}
</td>
    <td>
        {{   article.text }}
</td>
    <td>
     <span class="Parent">   <a href="{{ path('article-edit', {'id': article.id}) }}" class="btn btn-xs btn-success"><i class="fa fa-pencil" aria-hidden="true"></i></a>   </span>
    </td>
    <td>
      <span class="Parent">    <a href="{{ path('article_delete', {'id': article.id}) }}" class="btn btn-xs btn-success"><i class="fa fa-times" aria-hidden="true"></i></a> </span>
    </td>
        </tr>
    {%   endfor %}
</table>

    </div>

{% endblock %}